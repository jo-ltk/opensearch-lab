# Parses pino's JSON output. "time" is an ISO-8601 UTC timestamp because the
# API configures pino with stdTimeFunctions.isoTime.
[PARSER]
    Name        pino_json
    Format      json
    Time_Key    time
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ
